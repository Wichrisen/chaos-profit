# Хаос & Прибыль (Chaos & Profit)

Фановая некоммерческая игра про молодого предпринимателя в странном мире, где нужно крутить слот-машину, строить бизнесы и удерживать непредсказуемых клиентов, пока растущий Рейтисурд не разрушил всё.

**Жанр**: idle + слот-машина + management с элементами риска и хаоса.

## Текущий статус

**План утверждён** — 30 мая 2026.

Мы завершили основную фазу дизайна механик и перешли к реализации.

**Текущий этап:** Переход на Pygame GUI. Консольная версия в основном завершена. Сейчас делаем статичную, но уже playable визуальную версию (первый приоритет — слот как большая кнопка на дашборде).

- **SlotSystem** (95%): полностью реактивный к Рейтисурду, corrupted symbols, Chaotic Spins, Reality Fractures, Haunted businesses, меняющийся тон и анимация.
- **DealSystem** (90%): тёмные + искажённые сделки, вторичные проклятия, перманентные шрамы, титhe хаоса, драматические сообщения в зависимости от уровня хаоса.
- Полный цикл: Клонеты → слот/магазин → клиенты + Бизнета → сделки с риском → зелья (3 типа) → рост Рейтисурда как настоящая эскалация.
- Оффлайн-прогресс, апгрейды бизнесов, сохранения, сброс.

Прототип уже вызывает нужную эмоцию "всё хорошо... пока" и "что я наделал" на высоком хаосе.

### Как запустить игру

**Основная версия (Pygame GUI):**

```bash
python -m src.chaos_profit.ui_pygame
```

**Консольная версия** (для быстрого тестирования и баланса):

```bash
python -m src.chaos_profit.ui_console.console_app
```

**Доступные команды:**
- `status` / `s` — текущее состояние
- `advance <минуты>` / `a <минуты>` — продвинуть время (например `a 60`)
- Быстрые команды: `1m`, `5m`, `10m`, `30m`, `1h`, `2h`
- `deals` / `d` — показать текущую сделку
- `accept` / `refuse` — принять или отказаться от сделки
- `potions` / `p` — инвентарь зелий
- `use <тип>` — использовать зелье (например: use 10min, use permanent, use suppression)
- `shop` / `buy` — открыть магазин и купить новый бизнес
- `spin` / `slot` / 🎰 — крутить слот-машину (1 Клонета)
- `upgrades` / `up` + `upgrade <n> <growth|efficiency|resilience>` — улучшения бизнесов
- `reset` — полный сброс прогресса (с подтверждением)
- `upgrades` / `up` — посмотреть улучшения бизнесов
- `upgrade <номер> <тип>` — улучшить бизнес (growth / efficiency / resilience)
- `spin` / `slot` / 🎰 — крутить слот-машину (стоит 1 Клонету)
- `save`, `reset`, `help`, `quit`

Это временный инструмент для ручного тестирования механик.

В статусе теперь подробно показываются активные эффекты на каждом бизнесе + индикатор давления Рейтисурда (Calm → Extreme).

## Как запустить (пока)

```bash
cd "Projects/chaos&profit"

# Создать виртуальное окружение (рекомендуется)
python3 -m venv .venv
source .venv/bin/activate   # macOS / Linux
# .venv\Scripts\activate    # Windows

pip install -r requirements.txt
```

Пока нет playable-версии. Следите за обновлениями в `RECENTLY_MOVES.md`.

## Структура проекта

См. план разработки в `.grok/sessions/.../plan.md` (или в чате с Grok).

Ключевые папки:
- `docs/GDD.md` — главный документ с механиками и балансом (будет создан следующим)
- `src/chaos_profit/core/` — чистая логика (экономика, слот, хаос)
- `src/chaos_profit/ui_console/` — временный консольный интерфейс
- `src/chaos_profit/ui_pygame/` — основной 2D-интерфейс
- `tools/balance_sim.py` — симуляции для балансировки

## Цели первой версии (MVP)

- Полноценный core loop: Клонеты → слот → Бизнета → бизнесы → пассивный доход + хаос
- 5–8 бизнесов
- Рейтисурд 1–15 с реальным влиянием на риск/награду
- Сохранения + оффлайн-прогресс
- Играбельно 15–30 минут без багов

## Контакт / фидбек

Игра разрабатывается совместно с пользователем через Grok.  
Все важные решения фиксируются в `RECENTLY_MOVES.md`.

---

*Сделано с любовью к странным экономическим симуляторам.*
